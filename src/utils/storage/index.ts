export { LocalStorageManager } from './localStorage';
export { IndexedDBManager } from './indexedDB';
export type { StorageSettings, PageData, HistoryEntry } from './types';
export { DEFAULT_SETTINGS } from './types';
